name: ✅ Checks

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    test-books:
        name: 🧪✨ Test Books
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22

            - name: Install dependencies
              run: npm i ptbk

            - name: Test Books
              run: npx ptbk test "./**/*.book" --verbose
